dependencyResolutionManagement {
    repositoriesMode.set(repositoriesMode.set(RepositoriesMode.PREFER_SETTINGS))
    repositories {
        google()
        mavenCentral()
        jcenter()
    }
}
rootProject.name = "MMP-Pro"
include ':app'
